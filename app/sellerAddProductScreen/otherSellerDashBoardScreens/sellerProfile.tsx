import React from 'react';
import { ScrollView, View, Text } from "react-native";

const sellerProfile = () => {
  return (
    <ScrollView>
        <Text>profile</Text>
    </ScrollView>
  )
}

export default sellerProfile